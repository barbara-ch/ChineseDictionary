<%= form_for(@partofspeach) do |f| %>
  <% if @partofspeach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partofspeach.errors.count, "error") %> prohibited this partofspeach from being saved:</h2>

      <ul>
      <% @partofspeach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :short %><br>
    <%= f.text_field :short %>
  </div>
  <div class="actions">
    <%= f.submit "Dodaj" %>
  </div>
<% end %>
